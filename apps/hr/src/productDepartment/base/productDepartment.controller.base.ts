/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ProductDepartmentService } from "../productDepartment.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ProductDepartmentCreateInput } from "./ProductDepartmentCreateInput";
import { ProductDepartment } from "./ProductDepartment";
import { ProductDepartmentFindManyArgs } from "./ProductDepartmentFindManyArgs";
import { ProductDepartmentWhereUniqueInput } from "./ProductDepartmentWhereUniqueInput";
import { ProductDepartmentUpdateInput } from "./ProductDepartmentUpdateInput";
import { ProductFindManyArgs } from "../../product/base/ProductFindManyArgs";
import { Product } from "../../product/base/Product";
import { ProductWhereUniqueInput } from "../../product/base/ProductWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ProductDepartmentControllerBase {
  constructor(
    protected readonly service: ProductDepartmentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ProductDepartment })
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createProductDepartment(
    @common.Body() data: ProductDepartmentCreateInput
  ): Promise<ProductDepartment> {
    return await this.service.createProductDepartment({
      data: {
        ...data,

        parentProductDepartment: data.parentProductDepartment
          ? {
              connect: data.parentProductDepartment,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isDefault: true,
        name: true,
        normalizedName: true,
        note: true,

        parentProductDepartment: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ProductDepartment] })
  @ApiNestedQuery(ProductDepartmentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async productDepartments(
    @common.Req() request: Request
  ): Promise<ProductDepartment[]> {
    const args = plainToClass(ProductDepartmentFindManyArgs, request.query);
    return this.service.productDepartments({
      ...args,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isDefault: true,
        name: true,
        normalizedName: true,
        note: true,

        parentProductDepartment: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ProductDepartment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async productDepartment(
    @common.Param() params: ProductDepartmentWhereUniqueInput
  ): Promise<ProductDepartment | null> {
    const result = await this.service.productDepartment({
      where: params,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isDefault: true,
        name: true,
        normalizedName: true,
        note: true,

        parentProductDepartment: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ProductDepartment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateProductDepartment(
    @common.Param() params: ProductDepartmentWhereUniqueInput,
    @common.Body() data: ProductDepartmentUpdateInput
  ): Promise<ProductDepartment | null> {
    try {
      return await this.service.updateProductDepartment({
        where: params,
        data: {
          ...data,

          parentProductDepartment: data.parentProductDepartment
            ? {
                connect: data.parentProductDepartment,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isDefault: true,
          name: true,
          normalizedName: true,
          note: true,

          parentProductDepartment: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ProductDepartment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteProductDepartment(
    @common.Param() params: ProductDepartmentWhereUniqueInput
  ): Promise<ProductDepartment | null> {
    try {
      return await this.service.deleteProductDepartment({
        where: params,
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isDefault: true,
          name: true,
          normalizedName: true,
          note: true,

          parentProductDepartment: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/productDepartments")
  @ApiNestedQuery(ProductDepartmentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "read",
    possession: "any",
  })
  async findProductDepartments(
    @common.Req() request: Request,
    @common.Param() params: ProductDepartmentWhereUniqueInput
  ): Promise<ProductDepartment[]> {
    const query = plainToClass(ProductDepartmentFindManyArgs, request.query);
    const results = await this.service.findProductDepartments(params.id, {
      ...query,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isDefault: true,
        name: true,
        normalizedName: true,
        note: true,

        parentProductDepartment: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/productDepartments")
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "update",
    possession: "any",
  })
  async connectProductDepartments(
    @common.Param() params: ProductDepartmentWhereUniqueInput,
    @common.Body() body: ProductDepartmentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      productDepartments: {
        connect: body,
      },
    };
    await this.service.updateProductDepartment({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/productDepartments")
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "update",
    possession: "any",
  })
  async updateProductDepartments(
    @common.Param() params: ProductDepartmentWhereUniqueInput,
    @common.Body() body: ProductDepartmentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      productDepartments: {
        set: body,
      },
    };
    await this.service.updateProductDepartment({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/productDepartments")
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "update",
    possession: "any",
  })
  async disconnectProductDepartments(
    @common.Param() params: ProductDepartmentWhereUniqueInput,
    @common.Body() body: ProductDepartmentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      productDepartments: {
        disconnect: body,
      },
    };
    await this.service.updateProductDepartment({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/products")
  @ApiNestedQuery(ProductFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Product",
    action: "read",
    possession: "any",
  })
  async findProducts(
    @common.Req() request: Request,
    @common.Param() params: ProductDepartmentWhereUniqueInput
  ): Promise<Product[]> {
    const query = plainToClass(ProductFindManyArgs, request.query);
    const results = await this.service.findProducts(params.id, {
      ...query,
      select: {
        barcode: true,
        canExpire: true,
        code: true,
        costPrice: true,
        costPriceIncludesTax: true,
        createdAt: true,
        currentStockQuantity: true,
        daysToManufacture: true,

        defaultStoreId: {
          select: {
            id: true,
          },
        },

        defaultUnitId: {
          select: {
            id: true,
          },
        },

        description: true,
        discontinuedDate: true,
        discountRate: true,
        expireAlarmInDays: true,
        expireIsDefaultAfterDaysFromPurchase: true,
        firstStockQuantity: true,
        id: true,
        isActive: true,
        isCompound: true,
        isFavorite: true,
        maintainInventory: true,
        minimumSalePrice: true,
        name: true,
        normalizedName: true,
        note: true,
        photo: true,

        productCategoryId: {
          select: {
            id: true,
          },
        },

        productDepartmentId: {
          select: {
            id: true,
          },
        },

        productGroupId: {
          select: {
            id: true,
          },
        },

        productTypeId: {
          select: {
            id: true,
          },
        },

        profitRate: true,
        reorderQuantity: true,
        salePrice: true,
        salePriceIncludesTax: true,

        saleTaxId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/products")
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "update",
    possession: "any",
  })
  async connectProducts(
    @common.Param() params: ProductDepartmentWhereUniqueInput,
    @common.Body() body: ProductWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      products: {
        connect: body,
      },
    };
    await this.service.updateProductDepartment({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/products")
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "update",
    possession: "any",
  })
  async updateProducts(
    @common.Param() params: ProductDepartmentWhereUniqueInput,
    @common.Body() body: ProductWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      products: {
        set: body,
      },
    };
    await this.service.updateProductDepartment({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/products")
  @nestAccessControl.UseRoles({
    resource: "ProductDepartment",
    action: "update",
    possession: "any",
  })
  async disconnectProducts(
    @common.Param() params: ProductDepartmentWhereUniqueInput,
    @common.Body() body: ProductWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      products: {
        disconnect: body,
      },
    };
    await this.service.updateProductDepartment({
      where: params,
      data,
      select: { id: true },
    });
  }
}
