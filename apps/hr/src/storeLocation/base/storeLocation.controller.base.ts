/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { StoreLocationService } from "../storeLocation.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { StoreLocationCreateInput } from "./StoreLocationCreateInput";
import { StoreLocation } from "./StoreLocation";
import { StoreLocationFindManyArgs } from "./StoreLocationFindManyArgs";
import { StoreLocationWhereUniqueInput } from "./StoreLocationWhereUniqueInput";
import { StoreLocationUpdateInput } from "./StoreLocationUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class StoreLocationControllerBase {
  constructor(
    protected readonly service: StoreLocationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: StoreLocation })
  @nestAccessControl.UseRoles({
    resource: "StoreLocation",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createStoreLocation(
    @common.Body() data: StoreLocationCreateInput
  ): Promise<StoreLocation> {
    return await this.service.createStoreLocation({
      data: {
        ...data,

        storeId: {
          connect: data.storeId,
        },

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        createdAtSide: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        storeId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [StoreLocation] })
  @ApiNestedQuery(StoreLocationFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "StoreLocation",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async storeLocations(
    @common.Req() request: Request
  ): Promise<StoreLocation[]> {
    const args = plainToClass(StoreLocationFindManyArgs, request.query);
    return this.service.storeLocations({
      ...args,
      select: {
        code: true,
        createdAt: true,
        createdAtSide: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        storeId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: StoreLocation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StoreLocation",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async storeLocation(
    @common.Param() params: StoreLocationWhereUniqueInput
  ): Promise<StoreLocation | null> {
    const result = await this.service.storeLocation({
      where: params,
      select: {
        code: true,
        createdAt: true,
        createdAtSide: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        storeId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: StoreLocation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StoreLocation",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateStoreLocation(
    @common.Param() params: StoreLocationWhereUniqueInput,
    @common.Body() data: StoreLocationUpdateInput
  ): Promise<StoreLocation | null> {
    try {
      return await this.service.updateStoreLocation({
        where: params,
        data: {
          ...data,

          storeId: {
            connect: data.storeId,
          },

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          createdAtSide: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          storeId: {
            select: {
              id: true,
            },
          },

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: StoreLocation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StoreLocation",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteStoreLocation(
    @common.Param() params: StoreLocationWhereUniqueInput
  ): Promise<StoreLocation | null> {
    try {
      return await this.service.deleteStoreLocation({
        where: params,
        select: {
          code: true,
          createdAt: true,
          createdAtSide: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          storeId: {
            select: {
              id: true,
            },
          },

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
