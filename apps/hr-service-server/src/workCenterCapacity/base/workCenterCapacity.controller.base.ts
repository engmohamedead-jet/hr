/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WorkCenterCapacityService } from "../workCenterCapacity.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WorkCenterCapacityCreateInput } from "./WorkCenterCapacityCreateInput";
import { WorkCenterCapacity } from "./WorkCenterCapacity";
import { WorkCenterCapacityFindManyArgs } from "./WorkCenterCapacityFindManyArgs";
import { WorkCenterCapacityWhereUniqueInput } from "./WorkCenterCapacityWhereUniqueInput";
import { WorkCenterCapacityUpdateInput } from "./WorkCenterCapacityUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WorkCenterCapacityControllerBase {
  constructor(
    protected readonly service: WorkCenterCapacityService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: WorkCenterCapacity })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterCapacity",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createWorkCenterCapacity(
    @common.Body() data: WorkCenterCapacityCreateInput
  ): Promise<WorkCenterCapacity> {
    return await this.service.createWorkCenterCapacity({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,

        workCenterId: {
          connect: data.workCenterId,
        },
      },
      select: {
        capacity: true,
        createdAt: true,
        id: true,
        isActive: true,
        note: true,
        startTime: true,
        stopTime: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        workCenterId: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [WorkCenterCapacity] })
  @ApiNestedQuery(WorkCenterCapacityFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WorkCenterCapacity",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async workCenterCapacities(
    @common.Req() request: Request
  ): Promise<WorkCenterCapacity[]> {
    const args = plainToClass(WorkCenterCapacityFindManyArgs, request.query);
    return this.service.workCenterCapacities({
      ...args,
      select: {
        capacity: true,
        createdAt: true,
        id: true,
        isActive: true,
        note: true,
        startTime: true,
        stopTime: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        workCenterId: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: WorkCenterCapacity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterCapacity",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async workCenterCapacity(
    @common.Param() params: WorkCenterCapacityWhereUniqueInput
  ): Promise<WorkCenterCapacity | null> {
    const result = await this.service.workCenterCapacity({
      where: params,
      select: {
        capacity: true,
        createdAt: true,
        id: true,
        isActive: true,
        note: true,
        startTime: true,
        stopTime: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        workCenterId: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: WorkCenterCapacity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterCapacity",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateWorkCenterCapacity(
    @common.Param() params: WorkCenterCapacityWhereUniqueInput,
    @common.Body() data: WorkCenterCapacityUpdateInput
  ): Promise<WorkCenterCapacity | null> {
    try {
      return await this.service.updateWorkCenterCapacity({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,

          workCenterId: {
            connect: data.workCenterId,
          },
        },
        select: {
          capacity: true,
          createdAt: true,
          id: true,
          isActive: true,
          note: true,
          startTime: true,
          stopTime: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          workCenterId: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: WorkCenterCapacity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterCapacity",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteWorkCenterCapacity(
    @common.Param() params: WorkCenterCapacityWhereUniqueInput
  ): Promise<WorkCenterCapacity | null> {
    try {
      return await this.service.deleteWorkCenterCapacity({
        where: params,
        select: {
          capacity: true,
          createdAt: true,
          id: true,
          isActive: true,
          note: true,
          startTime: true,
          stopTime: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          workCenterId: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
