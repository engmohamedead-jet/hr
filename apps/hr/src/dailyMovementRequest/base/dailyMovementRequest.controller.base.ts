/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DailyMovementRequestService } from "../dailyMovementRequest.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DailyMovementRequestCreateInput } from "./DailyMovementRequestCreateInput";
import { DailyMovementRequest } from "./DailyMovementRequest";
import { DailyMovementRequestFindManyArgs } from "./DailyMovementRequestFindManyArgs";
import { DailyMovementRequestWhereUniqueInput } from "./DailyMovementRequestWhereUniqueInput";
import { DailyMovementRequestUpdateInput } from "./DailyMovementRequestUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DailyMovementRequestControllerBase {
  constructor(
    protected readonly service: DailyMovementRequestService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DailyMovementRequest })
  @nestAccessControl.UseRoles({
    resource: "DailyMovementRequest",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDailyMovementRequest(
    @common.Body() data: DailyMovementRequestCreateInput
  ): Promise<DailyMovementRequest> {
    return await this.service.createDailyMovementRequest({
      data: {
        ...data,

        approvedByUserId: data.approvedByUserId
          ? {
              connect: data.approvedByUserId,
            }
          : undefined,

        employeeId: {
          connect: data.employeeId,
        },

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,
        movementDate: true,
        note: true,
        reason: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        totalHours: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DailyMovementRequest] })
  @ApiNestedQuery(DailyMovementRequestFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DailyMovementRequest",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dailyMovementRequests(
    @common.Req() request: Request
  ): Promise<DailyMovementRequest[]> {
    const args = plainToClass(DailyMovementRequestFindManyArgs, request.query);
    return this.service.dailyMovementRequests({
      ...args,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,
        movementDate: true,
        note: true,
        reason: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        totalHours: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DailyMovementRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyMovementRequest",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dailyMovementRequest(
    @common.Param() params: DailyMovementRequestWhereUniqueInput
  ): Promise<DailyMovementRequest | null> {
    const result = await this.service.dailyMovementRequest({
      where: params,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,
        movementDate: true,
        note: true,
        reason: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        totalHours: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DailyMovementRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyMovementRequest",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDailyMovementRequest(
    @common.Param() params: DailyMovementRequestWhereUniqueInput,
    @common.Body() data: DailyMovementRequestUpdateInput
  ): Promise<DailyMovementRequest | null> {
    try {
      return await this.service.updateDailyMovementRequest({
        where: params,
        data: {
          ...data,

          approvedByUserId: data.approvedByUserId
            ? {
                connect: data.approvedByUserId,
              }
            : undefined,

          employeeId: {
            connect: data.employeeId,
          },

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          employeeId: {
            select: {
              id: true,
            },
          },

          id: true,
          isActive: true,
          isAppreoved: true,
          movementDate: true,
          note: true,
          reason: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          totalHours: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DailyMovementRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyMovementRequest",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDailyMovementRequest(
    @common.Param() params: DailyMovementRequestWhereUniqueInput
  ): Promise<DailyMovementRequest | null> {
    try {
      return await this.service.deleteDailyMovementRequest({
        where: params,
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          employeeId: {
            select: {
              id: true,
            },
          },

          id: true,
          isActive: true,
          isAppreoved: true,
          movementDate: true,
          note: true,
          reason: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          totalHours: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
