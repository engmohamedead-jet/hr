/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { SalaryItemGroupService } from "../salaryItemGroup.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SalaryItemGroupCreateInput } from "./SalaryItemGroupCreateInput";
import { SalaryItemGroup } from "./SalaryItemGroup";
import { SalaryItemGroupFindManyArgs } from "./SalaryItemGroupFindManyArgs";
import { SalaryItemGroupWhereUniqueInput } from "./SalaryItemGroupWhereUniqueInput";
import { SalaryItemGroupUpdateInput } from "./SalaryItemGroupUpdateInput";
import { SalaryItemFindManyArgs } from "../../salaryItem/base/SalaryItemFindManyArgs";
import { SalaryItem } from "../../salaryItem/base/SalaryItem";
import { SalaryItemWhereUniqueInput } from "../../salaryItem/base/SalaryItemWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SalaryItemGroupControllerBase {
  constructor(
    protected readonly service: SalaryItemGroupService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SalaryItemGroup })
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createSalaryItemGroup(
    @common.Body() data: SalaryItemGroupCreateInput
  ): Promise<SalaryItemGroup> {
    return await this.service.createSalaryItemGroup({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [SalaryItemGroup] })
  @ApiNestedQuery(SalaryItemGroupFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async salaryItemGroups(
    @common.Req() request: Request
  ): Promise<SalaryItemGroup[]> {
    const args = plainToClass(SalaryItemGroupFindManyArgs, request.query);
    return this.service.salaryItemGroups({
      ...args,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SalaryItemGroup })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async salaryItemGroup(
    @common.Param() params: SalaryItemGroupWhereUniqueInput
  ): Promise<SalaryItemGroup | null> {
    const result = await this.service.salaryItemGroup({
      where: params,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SalaryItemGroup })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateSalaryItemGroup(
    @common.Param() params: SalaryItemGroupWhereUniqueInput,
    @common.Body() data: SalaryItemGroupUpdateInput
  ): Promise<SalaryItemGroup | null> {
    try {
      return await this.service.updateSalaryItemGroup({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SalaryItemGroup })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteSalaryItemGroup(
    @common.Param() params: SalaryItemGroupWhereUniqueInput
  ): Promise<SalaryItemGroup | null> {
    try {
      return await this.service.deleteSalaryItemGroup({
        where: params,
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/salaryItems")
  @ApiNestedQuery(SalaryItemFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SalaryItem",
    action: "read",
    possession: "any",
  })
  async findSalaryItems(
    @common.Req() request: Request,
    @common.Param() params: SalaryItemGroupWhereUniqueInput
  ): Promise<SalaryItem[]> {
    const query = plainToClass(SalaryItemFindManyArgs, request.query);
    const results = await this.service.findSalaryItems(params.id, {
      ...query,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        salaryItemGroupId: {
          select: {
            id: true,
          },
        },

        salaryItemTypeId: {
          select: {
            id: true,
          },
        },

        salaryLawId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/salaryItems")
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "update",
    possession: "any",
  })
  async connectSalaryItems(
    @common.Param() params: SalaryItemGroupWhereUniqueInput,
    @common.Body() body: SalaryItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      salaryItems: {
        connect: body,
      },
    };
    await this.service.updateSalaryItemGroup({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/salaryItems")
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "update",
    possession: "any",
  })
  async updateSalaryItems(
    @common.Param() params: SalaryItemGroupWhereUniqueInput,
    @common.Body() body: SalaryItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      salaryItems: {
        set: body,
      },
    };
    await this.service.updateSalaryItemGroup({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/salaryItems")
  @nestAccessControl.UseRoles({
    resource: "SalaryItemGroup",
    action: "update",
    possession: "any",
  })
  async disconnectSalaryItems(
    @common.Param() params: SalaryItemGroupWhereUniqueInput,
    @common.Body() body: SalaryItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      salaryItems: {
        disconnect: body,
      },
    };
    await this.service.updateSalaryItemGroup({
      where: params,
      data,
      select: { id: true },
    });
  }
}
