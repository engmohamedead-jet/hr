/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TenantUser } from "./TenantUser";
import { TenantUserCountArgs } from "./TenantUserCountArgs";
import { TenantUserFindManyArgs } from "./TenantUserFindManyArgs";
import { TenantUserFindUniqueArgs } from "./TenantUserFindUniqueArgs";
import { DeleteTenantUserArgs } from "./DeleteTenantUserArgs";
import { TenantUserService } from "../tenantUser.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => TenantUser)
export class TenantUserResolverBase {
  constructor(
    protected readonly service: TenantUserService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "TenantUser",
    action: "read",
    possession: "any",
  })
  async _tenantUsersMeta(
    @graphql.Args() args: TenantUserCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [TenantUser])
  @nestAccessControl.UseRoles({
    resource: "TenantUser",
    action: "read",
    possession: "any",
  })
  async tenantUsers(
    @graphql.Args() args: TenantUserFindManyArgs
  ): Promise<TenantUser[]> {
    return this.service.tenantUsers(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => TenantUser, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "TenantUser",
    action: "read",
    possession: "own",
  })
  async tenantUser(
    @graphql.Args() args: TenantUserFindUniqueArgs
  ): Promise<TenantUser | null> {
    const result = await this.service.tenantUser(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => TenantUser)
  @nestAccessControl.UseRoles({
    resource: "TenantUser",
    action: "delete",
    possession: "any",
  })
  async deleteTenantUser(
    @graphql.Args() args: DeleteTenantUserArgs
  ): Promise<TenantUser | null> {
    try {
      return await this.service.deleteTenantUser(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
