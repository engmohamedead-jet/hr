/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DayOffRequestService } from "../dayOffRequest.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DayOffRequestCreateInput } from "./DayOffRequestCreateInput";
import { DayOffRequest } from "./DayOffRequest";
import { DayOffRequestFindManyArgs } from "./DayOffRequestFindManyArgs";
import { DayOffRequestWhereUniqueInput } from "./DayOffRequestWhereUniqueInput";
import { DayOffRequestUpdateInput } from "./DayOffRequestUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DayOffRequestControllerBase {
  constructor(
    protected readonly service: DayOffRequestService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DayOffRequest })
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDayOffRequest(
    @common.Body() data: DayOffRequestCreateInput
  ): Promise<DayOffRequest> {
    return await this.service.createDayOffRequest({
      data: {
        ...data,

        approvedByUserId: data.approvedByUserId
          ? {
              connect: data.approvedByUserId,
            }
          : undefined,

        employeeId: {
          connect: data.employeeId,
        },

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        dayOffDate: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DayOffRequest] })
  @ApiNestedQuery(DayOffRequestFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dayOffRequests(
    @common.Req() request: Request
  ): Promise<DayOffRequest[]> {
    const args = plainToClass(DayOffRequestFindManyArgs, request.query);
    return this.service.dayOffRequests({
      ...args,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        dayOffDate: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DayOffRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dayOffRequest(
    @common.Param() params: DayOffRequestWhereUniqueInput
  ): Promise<DayOffRequest | null> {
    const result = await this.service.dayOffRequest({
      where: params,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        dayOffDate: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DayOffRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDayOffRequest(
    @common.Param() params: DayOffRequestWhereUniqueInput,
    @common.Body() data: DayOffRequestUpdateInput
  ): Promise<DayOffRequest | null> {
    try {
      return await this.service.updateDayOffRequest({
        where: params,
        data: {
          ...data,

          approvedByUserId: data.approvedByUserId
            ? {
                connect: data.approvedByUserId,
              }
            : undefined,

          employeeId: {
            connect: data.employeeId,
          },

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          dayOffDate: true,

          employeeId: {
            select: {
              id: true,
            },
          },

          id: true,
          isActive: true,
          isAppreoved: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DayOffRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDayOffRequest(
    @common.Param() params: DayOffRequestWhereUniqueInput
  ): Promise<DayOffRequest | null> {
    try {
      return await this.service.deleteDayOffRequest({
        where: params,
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          dayOffDate: true,

          employeeId: {
            select: {
              id: true,
            },
          },

          id: true,
          isActive: true,
          isAppreoved: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
