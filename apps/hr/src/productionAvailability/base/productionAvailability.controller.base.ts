/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ProductionAvailabilityService } from "../productionAvailability.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ProductionAvailabilityCreateInput } from "./ProductionAvailabilityCreateInput";
import { ProductionAvailability } from "./ProductionAvailability";
import { ProductionAvailabilityFindManyArgs } from "./ProductionAvailabilityFindManyArgs";
import { ProductionAvailabilityWhereUniqueInput } from "./ProductionAvailabilityWhereUniqueInput";
import { ProductionAvailabilityUpdateInput } from "./ProductionAvailabilityUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ProductionAvailabilityControllerBase {
  constructor(
    protected readonly service: ProductionAvailabilityService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ProductionAvailability })
  @nestAccessControl.UseRoles({
    resource: "ProductionAvailability",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createProductionAvailability(
    @common.Body() data: ProductionAvailabilityCreateInput
  ): Promise<ProductionAvailability> {
    return await this.service.createProductionAvailability({
      data: data,
      select: {
        code: true,
        createdAt: true,
        id: true,
        name: true,
        normalizedName: true,
        note: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ProductionAvailability] })
  @ApiNestedQuery(ProductionAvailabilityFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ProductionAvailability",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async productionAvailabilities(
    @common.Req() request: Request
  ): Promise<ProductionAvailability[]> {
    const args = plainToClass(
      ProductionAvailabilityFindManyArgs,
      request.query
    );
    return this.service.productionAvailabilities({
      ...args,
      select: {
        code: true,
        createdAt: true,
        id: true,
        name: true,
        normalizedName: true,
        note: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ProductionAvailability })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductionAvailability",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async productionAvailability(
    @common.Param() params: ProductionAvailabilityWhereUniqueInput
  ): Promise<ProductionAvailability | null> {
    const result = await this.service.productionAvailability({
      where: params,
      select: {
        code: true,
        createdAt: true,
        id: true,
        name: true,
        normalizedName: true,
        note: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ProductionAvailability })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductionAvailability",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateProductionAvailability(
    @common.Param() params: ProductionAvailabilityWhereUniqueInput,
    @common.Body() data: ProductionAvailabilityUpdateInput
  ): Promise<ProductionAvailability | null> {
    try {
      return await this.service.updateProductionAvailability({
        where: params,
        data: data,
        select: {
          code: true,
          createdAt: true,
          id: true,
          name: true,
          normalizedName: true,
          note: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ProductionAvailability })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductionAvailability",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteProductionAvailability(
    @common.Param() params: ProductionAvailabilityWhereUniqueInput
  ): Promise<ProductionAvailability | null> {
    try {
      return await this.service.deleteProductionAvailability({
        where: params,
        select: {
          code: true,
          createdAt: true,
          id: true,
          name: true,
          normalizedName: true,
          note: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
