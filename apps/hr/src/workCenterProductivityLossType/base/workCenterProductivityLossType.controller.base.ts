/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WorkCenterProductivityLossTypeService } from "../workCenterProductivityLossType.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WorkCenterProductivityLossTypeCreateInput } from "./WorkCenterProductivityLossTypeCreateInput";
import { WorkCenterProductivityLossType } from "./WorkCenterProductivityLossType";
import { WorkCenterProductivityLossTypeFindManyArgs } from "./WorkCenterProductivityLossTypeFindManyArgs";
import { WorkCenterProductivityLossTypeWhereUniqueInput } from "./WorkCenterProductivityLossTypeWhereUniqueInput";
import { WorkCenterProductivityLossTypeUpdateInput } from "./WorkCenterProductivityLossTypeUpdateInput";
import { WorkCenterProductivityLossFindManyArgs } from "../../workCenterProductivityLoss/base/WorkCenterProductivityLossFindManyArgs";
import { WorkCenterProductivityLoss } from "../../workCenterProductivityLoss/base/WorkCenterProductivityLoss";
import { WorkCenterProductivityLossWhereUniqueInput } from "../../workCenterProductivityLoss/base/WorkCenterProductivityLossWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WorkCenterProductivityLossTypeControllerBase {
  constructor(
    protected readonly service: WorkCenterProductivityLossTypeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: WorkCenterProductivityLossType })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createWorkCenterProductivityLossType(
    @common.Body() data: WorkCenterProductivityLossTypeCreateInput
  ): Promise<WorkCenterProductivityLossType> {
    return await this.service.createWorkCenterProductivityLossType({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [WorkCenterProductivityLossType] })
  @ApiNestedQuery(WorkCenterProductivityLossTypeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async workCenterProductivityLossTypes(
    @common.Req() request: Request
  ): Promise<WorkCenterProductivityLossType[]> {
    const args = plainToClass(
      WorkCenterProductivityLossTypeFindManyArgs,
      request.query
    );
    return this.service.workCenterProductivityLossTypes({
      ...args,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: WorkCenterProductivityLossType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async workCenterProductivityLossType(
    @common.Param() params: WorkCenterProductivityLossTypeWhereUniqueInput
  ): Promise<WorkCenterProductivityLossType | null> {
    const result = await this.service.workCenterProductivityLossType({
      where: params,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: WorkCenterProductivityLossType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateWorkCenterProductivityLossType(
    @common.Param() params: WorkCenterProductivityLossTypeWhereUniqueInput,
    @common.Body() data: WorkCenterProductivityLossTypeUpdateInput
  ): Promise<WorkCenterProductivityLossType | null> {
    try {
      return await this.service.updateWorkCenterProductivityLossType({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: WorkCenterProductivityLossType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteWorkCenterProductivityLossType(
    @common.Param() params: WorkCenterProductivityLossTypeWhereUniqueInput
  ): Promise<WorkCenterProductivityLossType | null> {
    try {
      return await this.service.deleteWorkCenterProductivityLossType({
        where: params,
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/workCenterProductivityLosses")
  @ApiNestedQuery(WorkCenterProductivityLossFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLoss",
    action: "read",
    possession: "any",
  })
  async findWorkCenterProductivityLosses(
    @common.Req() request: Request,
    @common.Param() params: WorkCenterProductivityLossTypeWhereUniqueInput
  ): Promise<WorkCenterProductivityLoss[]> {
    const query = plainToClass(
      WorkCenterProductivityLossFindManyArgs,
      request.query
    );
    const results = await this.service.findWorkCenterProductivityLosses(
      params.id,
      {
        ...query,
        select: {
          code: true,
          createdAt: true,
          id: true,
          isActive: true,
          isManual: true,
          lossType: true,
          name: true,
          normalizedName: true,
          note: true,
          sequence: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          workCenterId: {
            select: {
              id: true,
            },
          },

          workCenterProductivityLossTypeI: {
            select: {
              id: true,
            },
          },
        },
      }
    );
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/workCenterProductivityLosses")
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "update",
    possession: "any",
  })
  async connectWorkCenterProductivityLosses(
    @common.Param() params: WorkCenterProductivityLossTypeWhereUniqueInput,
    @common.Body() body: WorkCenterProductivityLossWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      workCenterProductivityLosses: {
        connect: body,
      },
    };
    await this.service.updateWorkCenterProductivityLossType({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/workCenterProductivityLosses")
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "update",
    possession: "any",
  })
  async updateWorkCenterProductivityLosses(
    @common.Param() params: WorkCenterProductivityLossTypeWhereUniqueInput,
    @common.Body() body: WorkCenterProductivityLossWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      workCenterProductivityLosses: {
        set: body,
      },
    };
    await this.service.updateWorkCenterProductivityLossType({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/workCenterProductivityLosses")
  @nestAccessControl.UseRoles({
    resource: "WorkCenterProductivityLossType",
    action: "update",
    possession: "any",
  })
  async disconnectWorkCenterProductivityLosses(
    @common.Param() params: WorkCenterProductivityLossTypeWhereUniqueInput,
    @common.Body() body: WorkCenterProductivityLossWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      workCenterProductivityLosses: {
        disconnect: body,
      },
    };
    await this.service.updateWorkCenterProductivityLossType({
      where: params,
      data,
      select: { id: true },
    });
  }
}
