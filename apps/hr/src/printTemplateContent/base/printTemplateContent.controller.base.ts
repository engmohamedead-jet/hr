/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PrintTemplateContentService } from "../printTemplateContent.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PrintTemplateContentCreateInput } from "./PrintTemplateContentCreateInput";
import { PrintTemplateContent } from "./PrintTemplateContent";
import { PrintTemplateContentFindManyArgs } from "./PrintTemplateContentFindManyArgs";
import { PrintTemplateContentWhereUniqueInput } from "./PrintTemplateContentWhereUniqueInput";
import { PrintTemplateContentUpdateInput } from "./PrintTemplateContentUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PrintTemplateContentControllerBase {
  constructor(
    protected readonly service: PrintTemplateContentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PrintTemplateContent })
  @nestAccessControl.UseRoles({
    resource: "PrintTemplateContent",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPrintTemplateContent(
    @common.Body() data: PrintTemplateContentCreateInput
  ): Promise<PrintTemplateContent> {
    return await this.service.createPrintTemplateContent({
      data: {
        ...data,

        printTemplateId: data.printTemplateId
          ? {
              connect: data.printTemplateId,
            }
          : undefined,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        display: true,
        fieldValue: true,
        id: true,
        isActive: true,
        key: true,
        note: true,

        printTemplateId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PrintTemplateContent] })
  @ApiNestedQuery(PrintTemplateContentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PrintTemplateContent",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async printTemplateContents(
    @common.Req() request: Request
  ): Promise<PrintTemplateContent[]> {
    const args = plainToClass(PrintTemplateContentFindManyArgs, request.query);
    return this.service.printTemplateContents({
      ...args,
      select: {
        createdAt: true,
        display: true,
        fieldValue: true,
        id: true,
        isActive: true,
        key: true,
        note: true,

        printTemplateId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PrintTemplateContent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PrintTemplateContent",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async printTemplateContent(
    @common.Param() params: PrintTemplateContentWhereUniqueInput
  ): Promise<PrintTemplateContent | null> {
    const result = await this.service.printTemplateContent({
      where: params,
      select: {
        createdAt: true,
        display: true,
        fieldValue: true,
        id: true,
        isActive: true,
        key: true,
        note: true,

        printTemplateId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        value: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PrintTemplateContent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PrintTemplateContent",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePrintTemplateContent(
    @common.Param() params: PrintTemplateContentWhereUniqueInput,
    @common.Body() data: PrintTemplateContentUpdateInput
  ): Promise<PrintTemplateContent | null> {
    try {
      return await this.service.updatePrintTemplateContent({
        where: params,
        data: {
          ...data,

          printTemplateId: data.printTemplateId
            ? {
                connect: data.printTemplateId,
              }
            : undefined,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          display: true,
          fieldValue: true,
          id: true,
          isActive: true,
          key: true,
          note: true,

          printTemplateId: {
            select: {
              id: true,
            },
          },

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PrintTemplateContent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PrintTemplateContent",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePrintTemplateContent(
    @common.Param() params: PrintTemplateContentWhereUniqueInput
  ): Promise<PrintTemplateContent | null> {
    try {
      return await this.service.deletePrintTemplateContent({
        where: params,
        select: {
          createdAt: true,
          display: true,
          fieldValue: true,
          id: true,
          isActive: true,
          key: true,
          note: true,

          printTemplateId: {
            select: {
              id: true,
            },
          },

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
