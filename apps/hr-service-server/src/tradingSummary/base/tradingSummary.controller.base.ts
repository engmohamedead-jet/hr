/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TradingSummaryService } from "../tradingSummary.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TradingSummaryCreateInput } from "./TradingSummaryCreateInput";
import { TradingSummary } from "./TradingSummary";
import { TradingSummaryFindManyArgs } from "./TradingSummaryFindManyArgs";
import { TradingSummaryWhereUniqueInput } from "./TradingSummaryWhereUniqueInput";
import { TradingSummaryUpdateInput } from "./TradingSummaryUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TradingSummaryControllerBase {
  constructor(
    protected readonly service: TradingSummaryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TradingSummary })
  @nestAccessControl.UseRoles({
    resource: "TradingSummary",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createTradingSummary(
    @common.Body() data: TradingSummaryCreateInput
  ): Promise<TradingSummary> {
    return await this.service.createTradingSummary({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        fiscalWeekId: true,
        id: true,
        isActive: true,
        note: true,
        partyId: true,
        price: true,
        priceTotal: true,
        productId: true,
        productVariantId: true,
        quantity: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        transactionDate: true,
        transactionTypeId: true,
        unitId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TradingSummary] })
  @ApiNestedQuery(TradingSummaryFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TradingSummary",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async tradingSummaries(
    @common.Req() request: Request
  ): Promise<TradingSummary[]> {
    const args = plainToClass(TradingSummaryFindManyArgs, request.query);
    return this.service.tradingSummaries({
      ...args,
      select: {
        createdAt: true,
        fiscalWeekId: true,
        id: true,
        isActive: true,
        note: true,
        partyId: true,
        price: true,
        priceTotal: true,
        productId: true,
        productVariantId: true,
        quantity: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        transactionDate: true,
        transactionTypeId: true,
        unitId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TradingSummary })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TradingSummary",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async tradingSummary(
    @common.Param() params: TradingSummaryWhereUniqueInput
  ): Promise<TradingSummary | null> {
    const result = await this.service.tradingSummary({
      where: params,
      select: {
        createdAt: true,
        fiscalWeekId: true,
        id: true,
        isActive: true,
        note: true,
        partyId: true,
        price: true,
        priceTotal: true,
        productId: true,
        productVariantId: true,
        quantity: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        transactionDate: true,
        transactionTypeId: true,
        unitId: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TradingSummary })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TradingSummary",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateTradingSummary(
    @common.Param() params: TradingSummaryWhereUniqueInput,
    @common.Body() data: TradingSummaryUpdateInput
  ): Promise<TradingSummary | null> {
    try {
      return await this.service.updateTradingSummary({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          fiscalWeekId: true,
          id: true,
          isActive: true,
          note: true,
          partyId: true,
          price: true,
          priceTotal: true,
          productId: true,
          productVariantId: true,
          quantity: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          transactionDate: true,
          transactionTypeId: true,
          unitId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TradingSummary })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TradingSummary",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteTradingSummary(
    @common.Param() params: TradingSummaryWhereUniqueInput
  ): Promise<TradingSummary | null> {
    try {
      return await this.service.deleteTradingSummary({
        where: params,
        select: {
          createdAt: true,
          fiscalWeekId: true,
          id: true,
          isActive: true,
          note: true,
          partyId: true,
          price: true,
          priceTotal: true,
          productId: true,
          productVariantId: true,
          quantity: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          transactionDate: true,
          transactionTypeId: true,
          unitId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
