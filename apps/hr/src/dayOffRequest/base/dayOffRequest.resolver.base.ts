/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DayOffRequest } from "./DayOffRequest";
import { DayOffRequestCountArgs } from "./DayOffRequestCountArgs";
import { DayOffRequestFindManyArgs } from "./DayOffRequestFindManyArgs";
import { DayOffRequestFindUniqueArgs } from "./DayOffRequestFindUniqueArgs";
import { CreateDayOffRequestArgs } from "./CreateDayOffRequestArgs";
import { UpdateDayOffRequestArgs } from "./UpdateDayOffRequestArgs";
import { DeleteDayOffRequestArgs } from "./DeleteDayOffRequestArgs";
import { User } from "../../user/base/User";
import { Employee } from "../../employee/base/Employee";
import { Tenant } from "../../tenant/base/Tenant";
import { DayOffRequestService } from "../dayOffRequest.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DayOffRequest)
export class DayOffRequestResolverBase {
  constructor(
    protected readonly service: DayOffRequestService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "read",
    possession: "any",
  })
  async _dayOffRequestsMeta(
    @graphql.Args() args: DayOffRequestCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DayOffRequest])
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "read",
    possession: "any",
  })
  async dayOffRequests(
    @graphql.Args() args: DayOffRequestFindManyArgs
  ): Promise<DayOffRequest[]> {
    return this.service.dayOffRequests(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DayOffRequest, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "read",
    possession: "own",
  })
  async dayOffRequest(
    @graphql.Args() args: DayOffRequestFindUniqueArgs
  ): Promise<DayOffRequest | null> {
    const result = await this.service.dayOffRequest(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DayOffRequest)
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "create",
    possession: "any",
  })
  async createDayOffRequest(
    @graphql.Args() args: CreateDayOffRequestArgs
  ): Promise<DayOffRequest> {
    return await this.service.createDayOffRequest({
      ...args,
      data: {
        ...args.data,

        approvedByUserId: args.data.approvedByUserId
          ? {
              connect: args.data.approvedByUserId,
            }
          : undefined,

        employeeId: {
          connect: args.data.employeeId,
        },

        tenantId: args.data.tenantId
          ? {
              connect: args.data.tenantId,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DayOffRequest)
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "update",
    possession: "any",
  })
  async updateDayOffRequest(
    @graphql.Args() args: UpdateDayOffRequestArgs
  ): Promise<DayOffRequest | null> {
    try {
      return await this.service.updateDayOffRequest({
        ...args,
        data: {
          ...args.data,

          approvedByUserId: args.data.approvedByUserId
            ? {
                connect: args.data.approvedByUserId,
              }
            : undefined,

          employeeId: {
            connect: args.data.employeeId,
          },

          tenantId: args.data.tenantId
            ? {
                connect: args.data.tenantId,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DayOffRequest)
  @nestAccessControl.UseRoles({
    resource: "DayOffRequest",
    action: "delete",
    possession: "any",
  })
  async deleteDayOffRequest(
    @graphql.Args() args: DeleteDayOffRequestArgs
  ): Promise<DayOffRequest | null> {
    try {
      return await this.service.deleteDayOffRequest(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "approvedByUserId",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getApprovedByUserId(
    @graphql.Parent() parent: DayOffRequest
  ): Promise<User | null> {
    const result = await this.service.getApprovedByUserId(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Employee, {
    nullable: true,
    name: "employeeId",
  })
  @nestAccessControl.UseRoles({
    resource: "Employee",
    action: "read",
    possession: "any",
  })
  async getEmployeeId(
    @graphql.Parent() parent: DayOffRequest
  ): Promise<Employee | null> {
    const result = await this.service.getEmployeeId(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Tenant, {
    nullable: true,
    name: "tenantId",
  })
  @nestAccessControl.UseRoles({
    resource: "Tenant",
    action: "read",
    possession: "any",
  })
  async getTenantId(
    @graphql.Parent() parent: DayOffRequest
  ): Promise<Tenant | null> {
    const result = await this.service.getTenantId(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
