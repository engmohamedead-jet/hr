/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { LeaveRequestTypeService } from "../leaveRequestType.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { LeaveRequestTypeCreateInput } from "./LeaveRequestTypeCreateInput";
import { LeaveRequestType } from "./LeaveRequestType";
import { LeaveRequestTypeFindManyArgs } from "./LeaveRequestTypeFindManyArgs";
import { LeaveRequestTypeWhereUniqueInput } from "./LeaveRequestTypeWhereUniqueInput";
import { LeaveRequestTypeUpdateInput } from "./LeaveRequestTypeUpdateInput";
import { LeaveRequestFindManyArgs } from "../../leaveRequest/base/LeaveRequestFindManyArgs";
import { LeaveRequest } from "../../leaveRequest/base/LeaveRequest";
import { LeaveRequestWhereUniqueInput } from "../../leaveRequest/base/LeaveRequestWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class LeaveRequestTypeControllerBase {
  constructor(
    protected readonly service: LeaveRequestTypeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: LeaveRequestType })
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createLeaveRequestType(
    @common.Body() data: LeaveRequestTypeCreateInput
  ): Promise<LeaveRequestType> {
    return await this.service.createLeaveRequestType({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [LeaveRequestType] })
  @ApiNestedQuery(LeaveRequestTypeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async leaveRequestTypes(
    @common.Req() request: Request
  ): Promise<LeaveRequestType[]> {
    const args = plainToClass(LeaveRequestTypeFindManyArgs, request.query);
    return this.service.leaveRequestTypes({
      ...args,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: LeaveRequestType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async leaveRequestType(
    @common.Param() params: LeaveRequestTypeWhereUniqueInput
  ): Promise<LeaveRequestType | null> {
    const result = await this.service.leaveRequestType({
      where: params,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: LeaveRequestType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateLeaveRequestType(
    @common.Param() params: LeaveRequestTypeWhereUniqueInput,
    @common.Body() data: LeaveRequestTypeUpdateInput
  ): Promise<LeaveRequestType | null> {
    try {
      return await this.service.updateLeaveRequestType({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: LeaveRequestType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteLeaveRequestType(
    @common.Param() params: LeaveRequestTypeWhereUniqueInput
  ): Promise<LeaveRequestType | null> {
    try {
      return await this.service.deleteLeaveRequestType({
        where: params,
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/leavingPermissions")
  @ApiNestedQuery(LeaveRequestFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "LeaveRequest",
    action: "read",
    possession: "any",
  })
  async findLeavingPermissions(
    @common.Req() request: Request,
    @common.Param() params: LeaveRequestTypeWhereUniqueInput
  ): Promise<LeaveRequest[]> {
    const query = plainToClass(LeaveRequestFindManyArgs, request.query);
    const results = await this.service.findLeavingPermissions(params.id, {
      ...query,
      select: {
        LeaveDate: true,

        approvedByUserId: {
          select: {
            id: true,
          },
        },

        cause: true,
        createdAt: true,

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,

        leaveRequestTypeId: {
          select: {
            id: true,
          },
        },

        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/leavingPermissions")
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "update",
    possession: "any",
  })
  async connectLeavingPermissions(
    @common.Param() params: LeaveRequestTypeWhereUniqueInput,
    @common.Body() body: LeaveRequestWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      leavingPermissions: {
        connect: body,
      },
    };
    await this.service.updateLeaveRequestType({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/leavingPermissions")
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "update",
    possession: "any",
  })
  async updateLeavingPermissions(
    @common.Param() params: LeaveRequestTypeWhereUniqueInput,
    @common.Body() body: LeaveRequestWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      leavingPermissions: {
        set: body,
      },
    };
    await this.service.updateLeaveRequestType({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/leavingPermissions")
  @nestAccessControl.UseRoles({
    resource: "LeaveRequestType",
    action: "update",
    possession: "any",
  })
  async disconnectLeavingPermissions(
    @common.Param() params: LeaveRequestTypeWhereUniqueInput,
    @common.Body() body: LeaveRequestWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      leavingPermissions: {
        disconnect: body,
      },
    };
    await this.service.updateLeaveRequestType({
      where: params,
      data,
      select: { id: true },
    });
  }
}
