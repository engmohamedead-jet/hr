/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AdminRole } from "./AdminRole";
import { AdminRoleCountArgs } from "./AdminRoleCountArgs";
import { AdminRoleFindManyArgs } from "./AdminRoleFindManyArgs";
import { AdminRoleFindUniqueArgs } from "./AdminRoleFindUniqueArgs";
import { DeleteAdminRoleArgs } from "./DeleteAdminRoleArgs";
import { AdminRoleService } from "../adminRole.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => AdminRole)
export class AdminRoleResolverBase {
  constructor(
    protected readonly service: AdminRoleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "AdminRole",
    action: "read",
    possession: "any",
  })
  async _adminRolesMeta(
    @graphql.Args() args: AdminRoleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [AdminRole])
  @nestAccessControl.UseRoles({
    resource: "AdminRole",
    action: "read",
    possession: "any",
  })
  async adminRoles(
    @graphql.Args() args: AdminRoleFindManyArgs
  ): Promise<AdminRole[]> {
    return this.service.adminRoles(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => AdminRole, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "AdminRole",
    action: "read",
    possession: "own",
  })
  async adminRole(
    @graphql.Args() args: AdminRoleFindUniqueArgs
  ): Promise<AdminRole | null> {
    const result = await this.service.adminRole(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => AdminRole)
  @nestAccessControl.UseRoles({
    resource: "AdminRole",
    action: "delete",
    possession: "any",
  })
  async deleteAdminRole(
    @graphql.Args() args: DeleteAdminRoleArgs
  ): Promise<AdminRole | null> {
    try {
      return await this.service.deleteAdminRole(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
