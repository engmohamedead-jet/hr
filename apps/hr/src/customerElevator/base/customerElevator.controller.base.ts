/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CustomerElevatorService } from "../customerElevator.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CustomerElevatorCreateInput } from "./CustomerElevatorCreateInput";
import { CustomerElevator } from "./CustomerElevator";
import { CustomerElevatorFindManyArgs } from "./CustomerElevatorFindManyArgs";
import { CustomerElevatorWhereUniqueInput } from "./CustomerElevatorWhereUniqueInput";
import { CustomerElevatorUpdateInput } from "./CustomerElevatorUpdateInput";
import { FailureReportingFindManyArgs } from "../../failureReporting/base/FailureReportingFindManyArgs";
import { FailureReporting } from "../../failureReporting/base/FailureReporting";
import { FailureReportingWhereUniqueInput } from "../../failureReporting/base/FailureReportingWhereUniqueInput";
import { InstallationContractFindManyArgs } from "../../installationContract/base/InstallationContractFindManyArgs";
import { InstallationContract } from "../../installationContract/base/InstallationContract";
import { InstallationContractWhereUniqueInput } from "../../installationContract/base/InstallationContractWhereUniqueInput";
import { PeriodicMaintenanceOrderFindManyArgs } from "../../periodicMaintenanceOrder/base/PeriodicMaintenanceOrderFindManyArgs";
import { PeriodicMaintenanceOrder } from "../../periodicMaintenanceOrder/base/PeriodicMaintenanceOrder";
import { PeriodicMaintenanceOrderWhereUniqueInput } from "../../periodicMaintenanceOrder/base/PeriodicMaintenanceOrderWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CustomerElevatorControllerBase {
  constructor(
    protected readonly service: CustomerElevatorService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CustomerElevator })
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createCustomerElevator(
    @common.Body() data: CustomerElevatorCreateInput
  ): Promise<CustomerElevator> {
    return await this.service.createCustomerElevator({
      data: {
        ...data,

        elevatorId: {
          connect: data.elevatorId,
        },

        storeId: {
          connect: data.storeId,
        },
      },
      select: {
        address: true,
        amount: true,
        code: true,
        createdAt: true,

        elevatorId: {
          select: {
            id: true,
          },
        },

        hasPendingMaintenanceContractOrders: true,
        id: true,
        maintenanceContractDocumentImage: true,
        maintenanceEndDate: true,
        maintenanceStartDate: true,
        name: true,
        normalizedName: true,
        note: true,
        phoneNumber: true,

        storeId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CustomerElevator] })
  @ApiNestedQuery(CustomerElevatorFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async customerElevators(
    @common.Req() request: Request
  ): Promise<CustomerElevator[]> {
    const args = plainToClass(CustomerElevatorFindManyArgs, request.query);
    return this.service.customerElevators({
      ...args,
      select: {
        address: true,
        amount: true,
        code: true,
        createdAt: true,

        elevatorId: {
          select: {
            id: true,
          },
        },

        hasPendingMaintenanceContractOrders: true,
        id: true,
        maintenanceContractDocumentImage: true,
        maintenanceEndDate: true,
        maintenanceStartDate: true,
        name: true,
        normalizedName: true,
        note: true,
        phoneNumber: true,

        storeId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CustomerElevator })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async customerElevator(
    @common.Param() params: CustomerElevatorWhereUniqueInput
  ): Promise<CustomerElevator | null> {
    const result = await this.service.customerElevator({
      where: params,
      select: {
        address: true,
        amount: true,
        code: true,
        createdAt: true,

        elevatorId: {
          select: {
            id: true,
          },
        },

        hasPendingMaintenanceContractOrders: true,
        id: true,
        maintenanceContractDocumentImage: true,
        maintenanceEndDate: true,
        maintenanceStartDate: true,
        name: true,
        normalizedName: true,
        note: true,
        phoneNumber: true,

        storeId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CustomerElevator })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateCustomerElevator(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() data: CustomerElevatorUpdateInput
  ): Promise<CustomerElevator | null> {
    try {
      return await this.service.updateCustomerElevator({
        where: params,
        data: {
          ...data,

          elevatorId: {
            connect: data.elevatorId,
          },

          storeId: {
            connect: data.storeId,
          },
        },
        select: {
          address: true,
          amount: true,
          code: true,
          createdAt: true,

          elevatorId: {
            select: {
              id: true,
            },
          },

          hasPendingMaintenanceContractOrders: true,
          id: true,
          maintenanceContractDocumentImage: true,
          maintenanceEndDate: true,
          maintenanceStartDate: true,
          name: true,
          normalizedName: true,
          note: true,
          phoneNumber: true,

          storeId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CustomerElevator })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCustomerElevator(
    @common.Param() params: CustomerElevatorWhereUniqueInput
  ): Promise<CustomerElevator | null> {
    try {
      return await this.service.deleteCustomerElevator({
        where: params,
        select: {
          address: true,
          amount: true,
          code: true,
          createdAt: true,

          elevatorId: {
            select: {
              id: true,
            },
          },

          hasPendingMaintenanceContractOrders: true,
          id: true,
          maintenanceContractDocumentImage: true,
          maintenanceEndDate: true,
          maintenanceStartDate: true,
          name: true,
          normalizedName: true,
          note: true,
          phoneNumber: true,

          storeId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/failureReportings")
  @ApiNestedQuery(FailureReportingFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "FailureReporting",
    action: "read",
    possession: "any",
  })
  async findFailureReportings(
    @common.Req() request: Request,
    @common.Param() params: CustomerElevatorWhereUniqueInput
  ): Promise<FailureReporting[]> {
    const query = plainToClass(FailureReportingFindManyArgs, request.query);
    const results = await this.service.findFailureReportings(params.id, {
      ...query,
      select: {
        contactingFromTime: true,
        contactingToTime: true,
        createdAt: true,

        customerElevatorId: {
          select: {
            id: true,
          },
        },

        customerUser: {
          select: {
            id: true,
          },
        },

        elevator: {
          select: {
            id: true,
          },
        },

        failureDescription: true,

        failureTypeId: {
          select: {
            id: true,
          },
        },

        id: true,
        note: true,
        reportingDate: true,
        reviewDate: true,

        store: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        wasReviewed: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/failureReportings")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async connectFailureReportings(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: FailureReportingWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      failureReportings: {
        connect: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/failureReportings")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async updateFailureReportings(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: FailureReportingWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      failureReportings: {
        set: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/failureReportings")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async disconnectFailureReportings(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: FailureReportingWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      failureReportings: {
        disconnect: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/installationContracts")
  @ApiNestedQuery(InstallationContractFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "InstallationContract",
    action: "read",
    possession: "any",
  })
  async findInstallationContracts(
    @common.Req() request: Request,
    @common.Param() params: CustomerElevatorWhereUniqueInput
  ): Promise<InstallationContract[]> {
    const query = plainToClass(InstallationContractFindManyArgs, request.query);
    const results = await this.service.findInstallationContracts(params.id, {
      ...query,
      select: {
        address: true,
        amount: true,
        cabinetTechnician: true,
        contractDate: true,
        contractNumber: true,

        contractStatusId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        customerElevatorId: {
          select: {
            id: true,
          },
        },

        doorTechnician: true,
        electricityTechnician: true,

        elevatorId: {
          select: {
            id: true,
          },
        },

        elevatorsCount: true,
        id: true,
        phoneNumber: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/installationContracts")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async connectInstallationContracts(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: InstallationContractWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      installationContracts: {
        connect: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/installationContracts")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async updateInstallationContracts(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: InstallationContractWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      installationContracts: {
        set: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/installationContracts")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async disconnectInstallationContracts(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: InstallationContractWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      installationContracts: {
        disconnect: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/periodicMaintenanceOrders")
  @ApiNestedQuery(PeriodicMaintenanceOrderFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PeriodicMaintenanceOrder",
    action: "read",
    possession: "any",
  })
  async findPeriodicMaintenanceOrders(
    @common.Req() request: Request,
    @common.Param() params: CustomerElevatorWhereUniqueInput
  ): Promise<PeriodicMaintenanceOrder[]> {
    const query = plainToClass(
      PeriodicMaintenanceOrderFindManyArgs,
      request.query
    );
    const results = await this.service.findPeriodicMaintenanceOrders(
      params.id,
      {
        ...query,
        select: {
          confirmDate: true,
          contractEndDate: true,
          contractStartDate: true,
          createdAt: true,

          customerElevator: {
            select: {
              id: true,
            },
          },

          customerUserId: {
            select: {
              id: true,
            },
          },

          elevatorId: {
            select: {
              id: true,
            },
          },

          id: true,
          isConfirmed: true,
          orderDate: true,

          storeId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      }
    );
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/periodicMaintenanceOrders")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async connectPeriodicMaintenanceOrders(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: PeriodicMaintenanceOrderWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      periodicMaintenanceOrders: {
        connect: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/periodicMaintenanceOrders")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async updatePeriodicMaintenanceOrders(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: PeriodicMaintenanceOrderWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      periodicMaintenanceOrders: {
        set: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/periodicMaintenanceOrders")
  @nestAccessControl.UseRoles({
    resource: "CustomerElevator",
    action: "update",
    possession: "any",
  })
  async disconnectPeriodicMaintenanceOrders(
    @common.Param() params: CustomerElevatorWhereUniqueInput,
    @common.Body() body: PeriodicMaintenanceOrderWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      periodicMaintenanceOrders: {
        disconnect: body,
      },
    };
    await this.service.updateCustomerElevator({
      where: params,
      data,
      select: { id: true },
    });
  }
}
