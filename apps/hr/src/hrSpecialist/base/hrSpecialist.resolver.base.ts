/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { HrSpecialist } from "./HrSpecialist";
import { HrSpecialistCountArgs } from "./HrSpecialistCountArgs";
import { HrSpecialistFindManyArgs } from "./HrSpecialistFindManyArgs";
import { HrSpecialistFindUniqueArgs } from "./HrSpecialistFindUniqueArgs";
import { DeleteHrSpecialistArgs } from "./DeleteHrSpecialistArgs";
import { HrSpecialistService } from "../hrSpecialist.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => HrSpecialist)
export class HrSpecialistResolverBase {
  constructor(
    protected readonly service: HrSpecialistService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "HrSpecialist",
    action: "read",
    possession: "any",
  })
  async _hrSpecialistsMeta(
    @graphql.Args() args: HrSpecialistCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [HrSpecialist])
  @nestAccessControl.UseRoles({
    resource: "HrSpecialist",
    action: "read",
    possession: "any",
  })
  async hrSpecialists(
    @graphql.Args() args: HrSpecialistFindManyArgs
  ): Promise<HrSpecialist[]> {
    return this.service.hrSpecialists(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => HrSpecialist, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "HrSpecialist",
    action: "read",
    possession: "own",
  })
  async hrSpecialist(
    @graphql.Args() args: HrSpecialistFindUniqueArgs
  ): Promise<HrSpecialist | null> {
    const result = await this.service.hrSpecialist(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => HrSpecialist)
  @nestAccessControl.UseRoles({
    resource: "HrSpecialist",
    action: "delete",
    possession: "any",
  })
  async deleteHrSpecialist(
    @graphql.Args() args: DeleteHrSpecialistArgs
  ): Promise<HrSpecialist | null> {
    try {
      return await this.service.deleteHrSpecialist(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
