/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { SalaryLawService } from "../salaryLaw.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SalaryLawCreateInput } from "./SalaryLawCreateInput";
import { SalaryLaw } from "./SalaryLaw";
import { SalaryLawFindManyArgs } from "./SalaryLawFindManyArgs";
import { SalaryLawWhereUniqueInput } from "./SalaryLawWhereUniqueInput";
import { SalaryLawUpdateInput } from "./SalaryLawUpdateInput";
import { SalaryItemFindManyArgs } from "../../salaryItem/base/SalaryItemFindManyArgs";
import { SalaryItem } from "../../salaryItem/base/SalaryItem";
import { SalaryItemWhereUniqueInput } from "../../salaryItem/base/SalaryItemWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SalaryLawControllerBase {
  constructor(
    protected readonly service: SalaryLawService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SalaryLaw })
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createSalaryLaw(
    @common.Body() data: SalaryLawCreateInput
  ): Promise<SalaryLaw> {
    return await this.service.createSalaryLaw({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        description: true,
        effectiveDate: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [SalaryLaw] })
  @ApiNestedQuery(SalaryLawFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async salaryLaws(@common.Req() request: Request): Promise<SalaryLaw[]> {
    const args = plainToClass(SalaryLawFindManyArgs, request.query);
    return this.service.salaryLaws({
      ...args,
      select: {
        code: true,
        createdAt: true,
        description: true,
        effectiveDate: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SalaryLaw })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async salaryLaw(
    @common.Param() params: SalaryLawWhereUniqueInput
  ): Promise<SalaryLaw | null> {
    const result = await this.service.salaryLaw({
      where: params,
      select: {
        code: true,
        createdAt: true,
        description: true,
        effectiveDate: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SalaryLaw })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateSalaryLaw(
    @common.Param() params: SalaryLawWhereUniqueInput,
    @common.Body() data: SalaryLawUpdateInput
  ): Promise<SalaryLaw | null> {
    try {
      return await this.service.updateSalaryLaw({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          description: true,
          effectiveDate: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SalaryLaw })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteSalaryLaw(
    @common.Param() params: SalaryLawWhereUniqueInput
  ): Promise<SalaryLaw | null> {
    try {
      return await this.service.deleteSalaryLaw({
        where: params,
        select: {
          code: true,
          createdAt: true,
          description: true,
          effectiveDate: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/salaryItems")
  @ApiNestedQuery(SalaryItemFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SalaryItem",
    action: "read",
    possession: "any",
  })
  async findSalaryItems(
    @common.Req() request: Request,
    @common.Param() params: SalaryLawWhereUniqueInput
  ): Promise<SalaryItem[]> {
    const query = plainToClass(SalaryItemFindManyArgs, request.query);
    const results = await this.service.findSalaryItems(params.id, {
      ...query,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        salaryItemGroupId: {
          select: {
            id: true,
          },
        },

        salaryItemTypeId: {
          select: {
            id: true,
          },
        },

        salaryLawId: {
          select: {
            id: true,
          },
        },

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/salaryItems")
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "update",
    possession: "any",
  })
  async connectSalaryItems(
    @common.Param() params: SalaryLawWhereUniqueInput,
    @common.Body() body: SalaryItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      salaryItems: {
        connect: body,
      },
    };
    await this.service.updateSalaryLaw({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/salaryItems")
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "update",
    possession: "any",
  })
  async updateSalaryItems(
    @common.Param() params: SalaryLawWhereUniqueInput,
    @common.Body() body: SalaryItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      salaryItems: {
        set: body,
      },
    };
    await this.service.updateSalaryLaw({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/salaryItems")
  @nestAccessControl.UseRoles({
    resource: "SalaryLaw",
    action: "update",
    possession: "any",
  })
  async disconnectSalaryItems(
    @common.Param() params: SalaryLawWhereUniqueInput,
    @common.Body() body: SalaryItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      salaryItems: {
        disconnect: body,
      },
    };
    await this.service.updateSalaryLaw({
      where: params,
      data,
      select: { id: true },
    });
  }
}
