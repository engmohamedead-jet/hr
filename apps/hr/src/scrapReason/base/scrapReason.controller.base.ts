/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ScrapReasonService } from "../scrapReason.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ScrapReasonCreateInput } from "./ScrapReasonCreateInput";
import { ScrapReason } from "./ScrapReason";
import { ScrapReasonFindManyArgs } from "./ScrapReasonFindManyArgs";
import { ScrapReasonWhereUniqueInput } from "./ScrapReasonWhereUniqueInput";
import { ScrapReasonUpdateInput } from "./ScrapReasonUpdateInput";
import { WorkOrderFindManyArgs } from "../../workOrder/base/WorkOrderFindManyArgs";
import { WorkOrder } from "../../workOrder/base/WorkOrder";
import { WorkOrderWhereUniqueInput } from "../../workOrder/base/WorkOrderWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ScrapReasonControllerBase {
  constructor(
    protected readonly service: ScrapReasonService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ScrapReason })
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createScrapReason(
    @common.Body() data: ScrapReasonCreateInput
  ): Promise<ScrapReason> {
    return await this.service.createScrapReason({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ScrapReason] })
  @ApiNestedQuery(ScrapReasonFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async scrapReasons(@common.Req() request: Request): Promise<ScrapReason[]> {
    const args = plainToClass(ScrapReasonFindManyArgs, request.query);
    return this.service.scrapReasons({
      ...args,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ScrapReason })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async scrapReason(
    @common.Param() params: ScrapReasonWhereUniqueInput
  ): Promise<ScrapReason | null> {
    const result = await this.service.scrapReason({
      where: params,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ScrapReason })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateScrapReason(
    @common.Param() params: ScrapReasonWhereUniqueInput,
    @common.Body() data: ScrapReasonUpdateInput
  ): Promise<ScrapReason | null> {
    try {
      return await this.service.updateScrapReason({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ScrapReason })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteScrapReason(
    @common.Param() params: ScrapReasonWhereUniqueInput
  ): Promise<ScrapReason | null> {
    try {
      return await this.service.deleteScrapReason({
        where: params,
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/workOrders")
  @ApiNestedQuery(WorkOrderFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WorkOrder",
    action: "read",
    possession: "any",
  })
  async findWorkOrders(
    @common.Req() request: Request,
    @common.Param() params: ScrapReasonWhereUniqueInput
  ): Promise<WorkOrder[]> {
    const query = plainToClass(WorkOrderFindManyArgs, request.query);
    const results = await this.service.findWorkOrders(params.id, {
      ...query,
      select: {
        barcode: true,
        code: true,
        costsPerHour: true,
        createdAt: true,
        duaration: true,
        duarationExpected: true,
        duarationPercent: true,
        duarationUnit: true,
        dueDate: true,
        endDate: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,
        orderQuantity: true,
        productionDate: true,
        quantityProduced: true,
        quantityReportedFromPreviouseWorkOrder: true,

        scrapReasonId: {
          select: {
            id: true,
          },
        },

        scrappedQuantity: true,
        startDate: true,
        stockQuantity: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/workOrders")
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "update",
    possession: "any",
  })
  async connectWorkOrders(
    @common.Param() params: ScrapReasonWhereUniqueInput,
    @common.Body() body: WorkOrderWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      workOrders: {
        connect: body,
      },
    };
    await this.service.updateScrapReason({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/workOrders")
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "update",
    possession: "any",
  })
  async updateWorkOrders(
    @common.Param() params: ScrapReasonWhereUniqueInput,
    @common.Body() body: WorkOrderWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      workOrders: {
        set: body,
      },
    };
    await this.service.updateScrapReason({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/workOrders")
  @nestAccessControl.UseRoles({
    resource: "ScrapReason",
    action: "update",
    possession: "any",
  })
  async disconnectWorkOrders(
    @common.Param() params: ScrapReasonWhereUniqueInput,
    @common.Body() body: WorkOrderWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      workOrders: {
        disconnect: body,
      },
    };
    await this.service.updateScrapReason({
      where: params,
      data,
      select: { id: true },
    });
  }
}
