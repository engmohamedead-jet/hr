/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CheckInOutService } from "../checkInOut.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CheckInOutCreateInput } from "./CheckInOutCreateInput";
import { CheckInOut } from "./CheckInOut";
import { CheckInOutFindManyArgs } from "./CheckInOutFindManyArgs";
import { CheckInOutWhereUniqueInput } from "./CheckInOutWhereUniqueInput";
import { CheckInOutUpdateInput } from "./CheckInOutUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CheckInOutControllerBase {
  constructor(
    protected readonly service: CheckInOutService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CheckInOut })
  @nestAccessControl.UseRoles({
    resource: "CheckInOut",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createCheckInOut(
    @common.Body() data: CheckInOutCreateInput
  ): Promise<CheckInOut> {
    return await this.service.createCheckInOut({
      data: {
        ...data,

        approvedByUserId: data.approvedByUserId
          ? {
              connect: data.approvedByUserId,
            }
          : undefined,

        employee: {
          connect: data.employee,
        },

        machine: {
          connect: data.machine,
        },

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        checkDate: true,
        checkTime: true,
        checkType: true,
        createdAt: true,

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,

        machine: {
          select: {
            id: true,
          },
        },

        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CheckInOut] })
  @ApiNestedQuery(CheckInOutFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CheckInOut",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async checkInOuts(@common.Req() request: Request): Promise<CheckInOut[]> {
    const args = plainToClass(CheckInOutFindManyArgs, request.query);
    return this.service.checkInOuts({
      ...args,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        checkDate: true,
        checkTime: true,
        checkType: true,
        createdAt: true,

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,

        machine: {
          select: {
            id: true,
          },
        },

        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CheckInOut })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CheckInOut",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async checkInOut(
    @common.Param() params: CheckInOutWhereUniqueInput
  ): Promise<CheckInOut | null> {
    const result = await this.service.checkInOut({
      where: params,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        checkDate: true,
        checkTime: true,
        checkType: true,
        createdAt: true,

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        isAppreoved: true,

        machine: {
          select: {
            id: true,
          },
        },

        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CheckInOut })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CheckInOut",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateCheckInOut(
    @common.Param() params: CheckInOutWhereUniqueInput,
    @common.Body() data: CheckInOutUpdateInput
  ): Promise<CheckInOut | null> {
    try {
      return await this.service.updateCheckInOut({
        where: params,
        data: {
          ...data,

          approvedByUserId: data.approvedByUserId
            ? {
                connect: data.approvedByUserId,
              }
            : undefined,

          employee: {
            connect: data.employee,
          },

          machine: {
            connect: data.machine,
          },

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          checkDate: true,
          checkTime: true,
          checkType: true,
          createdAt: true,

          employee: {
            select: {
              id: true,
            },
          },

          id: true,
          isActive: true,
          isAppreoved: true,

          machine: {
            select: {
              id: true,
            },
          },

          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CheckInOut })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CheckInOut",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCheckInOut(
    @common.Param() params: CheckInOutWhereUniqueInput
  ): Promise<CheckInOut | null> {
    try {
      return await this.service.deleteCheckInOut({
        where: params,
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          checkDate: true,
          checkTime: true,
          checkType: true,
          createdAt: true,

          employee: {
            select: {
              id: true,
            },
          },

          id: true,
          isActive: true,
          isAppreoved: true,

          machine: {
            select: {
              id: true,
            },
          },

          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
