/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ExchangeRateDetailService } from "../exchangeRateDetail.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ExchangeRateDetailCreateInput } from "./ExchangeRateDetailCreateInput";
import { ExchangeRateDetail } from "./ExchangeRateDetail";
import { ExchangeRateDetailFindManyArgs } from "./ExchangeRateDetailFindManyArgs";
import { ExchangeRateDetailWhereUniqueInput } from "./ExchangeRateDetailWhereUniqueInput";
import { ExchangeRateDetailUpdateInput } from "./ExchangeRateDetailUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ExchangeRateDetailControllerBase {
  constructor(
    protected readonly service: ExchangeRateDetailService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ExchangeRateDetail })
  @nestAccessControl.UseRoles({
    resource: "ExchangeRateDetail",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createExchangeRateDetail(
    @common.Body() data: ExchangeRateDetailCreateInput
  ): Promise<ExchangeRateDetail> {
    return await this.service.createExchangeRateDetail({
      data: {
        ...data,

        foreignCurrencyId: data.foreignCurrencyId
          ? {
              connect: data.foreignCurrencyId,
            }
          : undefined,

        localCurrencyId: {
          connect: data.localCurrencyId,
        },
      },
      select: {
        createdAt: true,
        exchangeRateDate: true,
        exchangeRateValue: true,

        foreignCurrencyId: {
          select: {
            id: true,
          },
        },

        id: true,

        localCurrencyId: {
          select: {
            id: true,
          },
        },

        unit: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ExchangeRateDetail] })
  @ApiNestedQuery(ExchangeRateDetailFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ExchangeRateDetail",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async exchangeRateDetails(
    @common.Req() request: Request
  ): Promise<ExchangeRateDetail[]> {
    const args = plainToClass(ExchangeRateDetailFindManyArgs, request.query);
    return this.service.exchangeRateDetails({
      ...args,
      select: {
        createdAt: true,
        exchangeRateDate: true,
        exchangeRateValue: true,

        foreignCurrencyId: {
          select: {
            id: true,
          },
        },

        id: true,

        localCurrencyId: {
          select: {
            id: true,
          },
        },

        unit: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ExchangeRateDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ExchangeRateDetail",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async exchangeRateDetail(
    @common.Param() params: ExchangeRateDetailWhereUniqueInput
  ): Promise<ExchangeRateDetail | null> {
    const result = await this.service.exchangeRateDetail({
      where: params,
      select: {
        createdAt: true,
        exchangeRateDate: true,
        exchangeRateValue: true,

        foreignCurrencyId: {
          select: {
            id: true,
          },
        },

        id: true,

        localCurrencyId: {
          select: {
            id: true,
          },
        },

        unit: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ExchangeRateDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ExchangeRateDetail",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateExchangeRateDetail(
    @common.Param() params: ExchangeRateDetailWhereUniqueInput,
    @common.Body() data: ExchangeRateDetailUpdateInput
  ): Promise<ExchangeRateDetail | null> {
    try {
      return await this.service.updateExchangeRateDetail({
        where: params,
        data: {
          ...data,

          foreignCurrencyId: data.foreignCurrencyId
            ? {
                connect: data.foreignCurrencyId,
              }
            : undefined,

          localCurrencyId: {
            connect: data.localCurrencyId,
          },
        },
        select: {
          createdAt: true,
          exchangeRateDate: true,
          exchangeRateValue: true,

          foreignCurrencyId: {
            select: {
              id: true,
            },
          },

          id: true,

          localCurrencyId: {
            select: {
              id: true,
            },
          },

          unit: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ExchangeRateDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ExchangeRateDetail",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteExchangeRateDetail(
    @common.Param() params: ExchangeRateDetailWhereUniqueInput
  ): Promise<ExchangeRateDetail | null> {
    try {
      return await this.service.deleteExchangeRateDetail({
        where: params,
        select: {
          createdAt: true,
          exchangeRateDate: true,
          exchangeRateValue: true,

          foreignCurrencyId: {
            select: {
              id: true,
            },
          },

          id: true,

          localCurrencyId: {
            select: {
              id: true,
            },
          },

          unit: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
