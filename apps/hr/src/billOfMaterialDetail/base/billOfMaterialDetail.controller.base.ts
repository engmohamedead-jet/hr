/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BillOfMaterialDetailService } from "../billOfMaterialDetail.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BillOfMaterialDetailCreateInput } from "./BillOfMaterialDetailCreateInput";
import { BillOfMaterialDetail } from "./BillOfMaterialDetail";
import { BillOfMaterialDetailFindManyArgs } from "./BillOfMaterialDetailFindManyArgs";
import { BillOfMaterialDetailWhereUniqueInput } from "./BillOfMaterialDetailWhereUniqueInput";
import { BillOfMaterialDetailUpdateInput } from "./BillOfMaterialDetailUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BillOfMaterialDetailControllerBase {
  constructor(
    protected readonly service: BillOfMaterialDetailService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BillOfMaterialDetail })
  @nestAccessControl.UseRoles({
    resource: "BillOfMaterialDetail",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBillOfMaterialDetail(
    @common.Body() data: BillOfMaterialDetailCreateInput
  ): Promise<BillOfMaterialDetail> {
    return await this.service.createBillOfMaterialDetail({
      data: {
        ...data,

        billOfMaterial: {
          connect: data.billOfMaterial,
        },

        productId: {
          connect: data.productId,
        },

        productVariantId: data.productVariantId
          ? {
              connect: data.productVariantId,
            }
          : undefined,

        unitId: {
          connect: data.unitId,
        },

        workCenterRoutingId: data.workCenterRoutingId
          ? {
              connect: data.workCenterRoutingId,
            }
          : undefined,
      },
      select: {
        billOfMaterial: {
          select: {
            id: true,
          },
        },

        costShare: true,
        createdAt: true,
        id: true,
        isActive: true,
        isManualConsumption: true,
        note: true,

        productId: {
          select: {
            id: true,
          },
        },

        productVariantId: {
          select: {
            id: true,
          },
        },

        quantity: true,
        sequence: true,

        unitId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        workCenterRoutingId: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BillOfMaterialDetail] })
  @ApiNestedQuery(BillOfMaterialDetailFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BillOfMaterialDetail",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billOfMaterialDetails(
    @common.Req() request: Request
  ): Promise<BillOfMaterialDetail[]> {
    const args = plainToClass(BillOfMaterialDetailFindManyArgs, request.query);
    return this.service.billOfMaterialDetails({
      ...args,
      select: {
        billOfMaterial: {
          select: {
            id: true,
          },
        },

        costShare: true,
        createdAt: true,
        id: true,
        isActive: true,
        isManualConsumption: true,
        note: true,

        productId: {
          select: {
            id: true,
          },
        },

        productVariantId: {
          select: {
            id: true,
          },
        },

        quantity: true,
        sequence: true,

        unitId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        workCenterRoutingId: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BillOfMaterialDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillOfMaterialDetail",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billOfMaterialDetail(
    @common.Param() params: BillOfMaterialDetailWhereUniqueInput
  ): Promise<BillOfMaterialDetail | null> {
    const result = await this.service.billOfMaterialDetail({
      where: params,
      select: {
        billOfMaterial: {
          select: {
            id: true,
          },
        },

        costShare: true,
        createdAt: true,
        id: true,
        isActive: true,
        isManualConsumption: true,
        note: true,

        productId: {
          select: {
            id: true,
          },
        },

        productVariantId: {
          select: {
            id: true,
          },
        },

        quantity: true,
        sequence: true,

        unitId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        workCenterRoutingId: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BillOfMaterialDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillOfMaterialDetail",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBillOfMaterialDetail(
    @common.Param() params: BillOfMaterialDetailWhereUniqueInput,
    @common.Body() data: BillOfMaterialDetailUpdateInput
  ): Promise<BillOfMaterialDetail | null> {
    try {
      return await this.service.updateBillOfMaterialDetail({
        where: params,
        data: {
          ...data,

          billOfMaterial: {
            connect: data.billOfMaterial,
          },

          productId: {
            connect: data.productId,
          },

          productVariantId: data.productVariantId
            ? {
                connect: data.productVariantId,
              }
            : undefined,

          unitId: {
            connect: data.unitId,
          },

          workCenterRoutingId: data.workCenterRoutingId
            ? {
                connect: data.workCenterRoutingId,
              }
            : undefined,
        },
        select: {
          billOfMaterial: {
            select: {
              id: true,
            },
          },

          costShare: true,
          createdAt: true,
          id: true,
          isActive: true,
          isManualConsumption: true,
          note: true,

          productId: {
            select: {
              id: true,
            },
          },

          productVariantId: {
            select: {
              id: true,
            },
          },

          quantity: true,
          sequence: true,

          unitId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          workCenterRoutingId: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BillOfMaterialDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillOfMaterialDetail",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBillOfMaterialDetail(
    @common.Param() params: BillOfMaterialDetailWhereUniqueInput
  ): Promise<BillOfMaterialDetail | null> {
    try {
      return await this.service.deleteBillOfMaterialDetail({
        where: params,
        select: {
          billOfMaterial: {
            select: {
              id: true,
            },
          },

          costShare: true,
          createdAt: true,
          id: true,
          isActive: true,
          isManualConsumption: true,
          note: true,

          productId: {
            select: {
              id: true,
            },
          },

          productVariantId: {
            select: {
              id: true,
            },
          },

          quantity: true,
          sequence: true,

          unitId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          workCenterRoutingId: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
