/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { StoreTypeService } from "../storeType.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { StoreTypeCreateInput } from "./StoreTypeCreateInput";
import { StoreType } from "./StoreType";
import { StoreTypeFindManyArgs } from "./StoreTypeFindManyArgs";
import { StoreTypeWhereUniqueInput } from "./StoreTypeWhereUniqueInput";
import { StoreTypeUpdateInput } from "./StoreTypeUpdateInput";
import { StoreFindManyArgs } from "../../store/base/StoreFindManyArgs";
import { Store } from "../../store/base/Store";
import { StoreWhereUniqueInput } from "../../store/base/StoreWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class StoreTypeControllerBase {
  constructor(
    protected readonly service: StoreTypeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: StoreType })
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createStoreType(
    @common.Body() data: StoreTypeCreateInput
  ): Promise<StoreType> {
    return await this.service.createStoreType({
      data: {
        ...data,

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        isDefault: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [StoreType] })
  @ApiNestedQuery(StoreTypeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async storeTypes(@common.Req() request: Request): Promise<StoreType[]> {
    const args = plainToClass(StoreTypeFindManyArgs, request.query);
    return this.service.storeTypes({
      ...args,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        isDefault: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: StoreType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async storeType(
    @common.Param() params: StoreTypeWhereUniqueInput
  ): Promise<StoreType | null> {
    const result = await this.service.storeType({
      where: params,
      select: {
        code: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        isDefault: true,
        name: true,
        normalizedName: true,
        note: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: StoreType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateStoreType(
    @common.Param() params: StoreTypeWhereUniqueInput,
    @common.Body() data: StoreTypeUpdateInput
  ): Promise<StoreType | null> {
    try {
      return await this.service.updateStoreType({
        where: params,
        data: {
          ...data,

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          isDefault: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: StoreType })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteStoreType(
    @common.Param() params: StoreTypeWhereUniqueInput
  ): Promise<StoreType | null> {
    try {
      return await this.service.deleteStoreType({
        where: params,
        select: {
          code: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          isDefault: true,
          name: true,
          normalizedName: true,
          note: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/stores")
  @ApiNestedQuery(StoreFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Store",
    action: "read",
    possession: "any",
  })
  async findStores(
    @common.Req() request: Request,
    @common.Param() params: StoreTypeWhereUniqueInput
  ): Promise<Store[]> {
    const query = plainToClass(StoreFindManyArgs, request.query);
    const results = await this.service.findStores(params.id, {
      ...query,
      select: {
        addressLine1: true,
        allowSale: true,
        cellPhoneNumber: true,
        code: true,
        createdAt: true,
        fax: true,
        homePhoneNumber: true,
        id: true,
        isActive: true,
        name: true,
        normalizedName: true,
        note: true,

        storeTypId: {
          select: {
            id: true,
          },
        },

        street: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/stores")
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "update",
    possession: "any",
  })
  async connectStores(
    @common.Param() params: StoreTypeWhereUniqueInput,
    @common.Body() body: StoreWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      stores: {
        connect: body,
      },
    };
    await this.service.updateStoreType({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/stores")
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "update",
    possession: "any",
  })
  async updateStores(
    @common.Param() params: StoreTypeWhereUniqueInput,
    @common.Body() body: StoreWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      stores: {
        set: body,
      },
    };
    await this.service.updateStoreType({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/stores")
  @nestAccessControl.UseRoles({
    resource: "StoreType",
    action: "update",
    possession: "any",
  })
  async disconnectStores(
    @common.Param() params: StoreTypeWhereUniqueInput,
    @common.Body() body: StoreWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      stores: {
        disconnect: body,
      },
    };
    await this.service.updateStoreType({
      where: params,
      data,
      select: { id: true },
    });
  }
}
