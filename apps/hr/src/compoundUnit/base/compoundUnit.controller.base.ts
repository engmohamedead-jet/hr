/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CompoundUnitService } from "../compoundUnit.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CompoundUnitCreateInput } from "./CompoundUnitCreateInput";
import { CompoundUnit } from "./CompoundUnit";
import { CompoundUnitFindManyArgs } from "./CompoundUnitFindManyArgs";
import { CompoundUnitWhereUniqueInput } from "./CompoundUnitWhereUniqueInput";
import { CompoundUnitUpdateInput } from "./CompoundUnitUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CompoundUnitControllerBase {
  constructor(
    protected readonly service: CompoundUnitService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CompoundUnit })
  @nestAccessControl.UseRoles({
    resource: "CompoundUnit",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createCompoundUnit(
    @common.Body() data: CompoundUnitCreateInput
  ): Promise<CompoundUnit> {
    return await this.service.createCompoundUnit({
      data: {
        ...data,

        baseUnitId: {
          connect: data.baseUnitId,
        },

        compareUnitId: {
          connect: data.compareUnitId,
        },
      },
      select: {
        baseUnitId: {
          select: {
            id: true,
          },
        },

        compareUnitId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        notes: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CompoundUnit] })
  @ApiNestedQuery(CompoundUnitFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CompoundUnit",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async compoundUnits(@common.Req() request: Request): Promise<CompoundUnit[]> {
    const args = plainToClass(CompoundUnitFindManyArgs, request.query);
    return this.service.compoundUnits({
      ...args,
      select: {
        baseUnitId: {
          select: {
            id: true,
          },
        },

        compareUnitId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        notes: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CompoundUnit })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CompoundUnit",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async compoundUnit(
    @common.Param() params: CompoundUnitWhereUniqueInput
  ): Promise<CompoundUnit | null> {
    const result = await this.service.compoundUnit({
      where: params,
      select: {
        baseUnitId: {
          select: {
            id: true,
          },
        },

        compareUnitId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        notes: true,
        updatedAt: true,
        value: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CompoundUnit })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CompoundUnit",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateCompoundUnit(
    @common.Param() params: CompoundUnitWhereUniqueInput,
    @common.Body() data: CompoundUnitUpdateInput
  ): Promise<CompoundUnit | null> {
    try {
      return await this.service.updateCompoundUnit({
        where: params,
        data: {
          ...data,

          baseUnitId: {
            connect: data.baseUnitId,
          },

          compareUnitId: {
            connect: data.compareUnitId,
          },
        },
        select: {
          baseUnitId: {
            select: {
              id: true,
            },
          },

          compareUnitId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          notes: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CompoundUnit })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CompoundUnit",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCompoundUnit(
    @common.Param() params: CompoundUnitWhereUniqueInput
  ): Promise<CompoundUnit | null> {
    try {
      return await this.service.deleteCompoundUnit({
        where: params,
        select: {
          baseUnitId: {
            select: {
              id: true,
            },
          },

          compareUnitId: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          notes: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
