/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { InstallationContractService } from "../installationContract.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { InstallationContractCreateInput } from "./InstallationContractCreateInput";
import { InstallationContract } from "./InstallationContract";
import { InstallationContractFindManyArgs } from "./InstallationContractFindManyArgs";
import { InstallationContractWhereUniqueInput } from "./InstallationContractWhereUniqueInput";
import { InstallationContractUpdateInput } from "./InstallationContractUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class InstallationContractControllerBase {
  constructor(
    protected readonly service: InstallationContractService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: InstallationContract })
  @nestAccessControl.UseRoles({
    resource: "InstallationContract",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createInstallationContract(
    @common.Body() data: InstallationContractCreateInput
  ): Promise<InstallationContract> {
    return await this.service.createInstallationContract({
      data: {
        ...data,

        contractStatusId: {
          connect: data.contractStatusId,
        },

        customerElevatorId: {
          connect: data.customerElevatorId,
        },

        elevatorId: {
          connect: data.elevatorId,
        },
      },
      select: {
        address: true,
        amount: true,
        cabinetTechnician: true,
        contractDate: true,
        contractNumber: true,

        contractStatusId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        customerElevatorId: {
          select: {
            id: true,
          },
        },

        doorTechnician: true,
        electricityTechnician: true,

        elevatorId: {
          select: {
            id: true,
          },
        },

        elevatorsCount: true,
        id: true,
        phoneNumber: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [InstallationContract] })
  @ApiNestedQuery(InstallationContractFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "InstallationContract",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async installationContracts(
    @common.Req() request: Request
  ): Promise<InstallationContract[]> {
    const args = plainToClass(InstallationContractFindManyArgs, request.query);
    return this.service.installationContracts({
      ...args,
      select: {
        address: true,
        amount: true,
        cabinetTechnician: true,
        contractDate: true,
        contractNumber: true,

        contractStatusId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        customerElevatorId: {
          select: {
            id: true,
          },
        },

        doorTechnician: true,
        electricityTechnician: true,

        elevatorId: {
          select: {
            id: true,
          },
        },

        elevatorsCount: true,
        id: true,
        phoneNumber: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: InstallationContract })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "InstallationContract",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async installationContract(
    @common.Param() params: InstallationContractWhereUniqueInput
  ): Promise<InstallationContract | null> {
    const result = await this.service.installationContract({
      where: params,
      select: {
        address: true,
        amount: true,
        cabinetTechnician: true,
        contractDate: true,
        contractNumber: true,

        contractStatusId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        customerElevatorId: {
          select: {
            id: true,
          },
        },

        doorTechnician: true,
        electricityTechnician: true,

        elevatorId: {
          select: {
            id: true,
          },
        },

        elevatorsCount: true,
        id: true,
        phoneNumber: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: InstallationContract })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "InstallationContract",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateInstallationContract(
    @common.Param() params: InstallationContractWhereUniqueInput,
    @common.Body() data: InstallationContractUpdateInput
  ): Promise<InstallationContract | null> {
    try {
      return await this.service.updateInstallationContract({
        where: params,
        data: {
          ...data,

          contractStatusId: {
            connect: data.contractStatusId,
          },

          customerElevatorId: {
            connect: data.customerElevatorId,
          },

          elevatorId: {
            connect: data.elevatorId,
          },
        },
        select: {
          address: true,
          amount: true,
          cabinetTechnician: true,
          contractDate: true,
          contractNumber: true,

          contractStatusId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          customerElevatorId: {
            select: {
              id: true,
            },
          },

          doorTechnician: true,
          electricityTechnician: true,

          elevatorId: {
            select: {
              id: true,
            },
          },

          elevatorsCount: true,
          id: true,
          phoneNumber: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: InstallationContract })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "InstallationContract",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteInstallationContract(
    @common.Param() params: InstallationContractWhereUniqueInput
  ): Promise<InstallationContract | null> {
    try {
      return await this.service.deleteInstallationContract({
        where: params,
        select: {
          address: true,
          amount: true,
          cabinetTechnician: true,
          contractDate: true,
          contractNumber: true,

          contractStatusId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          customerElevatorId: {
            select: {
              id: true,
            },
          },

          doorTechnician: true,
          electricityTechnician: true,

          elevatorId: {
            select: {
              id: true,
            },
          },

          elevatorsCount: true,
          id: true,
          phoneNumber: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
