/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { OverNightRequestService } from "../overNightRequest.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { OverNightRequestCreateInput } from "./OverNightRequestCreateInput";
import { OverNightRequest } from "./OverNightRequest";
import { OverNightRequestFindManyArgs } from "./OverNightRequestFindManyArgs";
import { OverNightRequestWhereUniqueInput } from "./OverNightRequestWhereUniqueInput";
import { OverNightRequestUpdateInput } from "./OverNightRequestUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class OverNightRequestControllerBase {
  constructor(
    protected readonly service: OverNightRequestService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: OverNightRequest })
  @nestAccessControl.UseRoles({
    resource: "OverNightRequest",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createOverNightRequest(
    @common.Body() data: OverNightRequestCreateInput
  ): Promise<OverNightRequest> {
    return await this.service.createOverNightRequest({
      data: {
        ...data,

        approvedByUserId: data.approvedByUserId
          ? {
              connect: data.approvedByUserId,
            }
          : undefined,

        employeeId: {
          connect: data.employeeId,
        },

        tenantId: data.tenantId
          ? {
              connect: data.tenantId,
            }
          : undefined,
      },
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        endDateTime: true,
        id: true,
        isActive: true,
        isAppreoved: true,
        note: true,
        startDateTime: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [OverNightRequest] })
  @ApiNestedQuery(OverNightRequestFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "OverNightRequest",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async overNightRequests(
    @common.Req() request: Request
  ): Promise<OverNightRequest[]> {
    const args = plainToClass(OverNightRequestFindManyArgs, request.query);
    return this.service.overNightRequests({
      ...args,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        endDateTime: true,
        id: true,
        isActive: true,
        isAppreoved: true,
        note: true,
        startDateTime: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: OverNightRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OverNightRequest",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async overNightRequest(
    @common.Param() params: OverNightRequestWhereUniqueInput
  ): Promise<OverNightRequest | null> {
    const result = await this.service.overNightRequest({
      where: params,
      select: {
        approvedByUserId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        employeeId: {
          select: {
            id: true,
          },
        },

        endDateTime: true,
        id: true,
        isActive: true,
        isAppreoved: true,
        note: true,
        startDateTime: true,

        tenantId: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: OverNightRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OverNightRequest",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateOverNightRequest(
    @common.Param() params: OverNightRequestWhereUniqueInput,
    @common.Body() data: OverNightRequestUpdateInput
  ): Promise<OverNightRequest | null> {
    try {
      return await this.service.updateOverNightRequest({
        where: params,
        data: {
          ...data,

          approvedByUserId: data.approvedByUserId
            ? {
                connect: data.approvedByUserId,
              }
            : undefined,

          employeeId: {
            connect: data.employeeId,
          },

          tenantId: data.tenantId
            ? {
                connect: data.tenantId,
              }
            : undefined,
        },
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          employeeId: {
            select: {
              id: true,
            },
          },

          endDateTime: true,
          id: true,
          isActive: true,
          isAppreoved: true,
          note: true,
          startDateTime: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: OverNightRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OverNightRequest",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteOverNightRequest(
    @common.Param() params: OverNightRequestWhereUniqueInput
  ): Promise<OverNightRequest | null> {
    try {
      return await this.service.deleteOverNightRequest({
        where: params,
        select: {
          approvedByUserId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          employeeId: {
            select: {
              id: true,
            },
          },

          endDateTime: true,
          id: true,
          isActive: true,
          isAppreoved: true,
          note: true,
          startDateTime: true,

          tenantId: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
