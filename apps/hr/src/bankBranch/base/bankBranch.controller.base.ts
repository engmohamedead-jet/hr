/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BankBranchService } from "../bankBranch.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BankBranchCreateInput } from "./BankBranchCreateInput";
import { BankBranch } from "./BankBranch";
import { BankBranchFindManyArgs } from "./BankBranchFindManyArgs";
import { BankBranchWhereUniqueInput } from "./BankBranchWhereUniqueInput";
import { BankBranchUpdateInput } from "./BankBranchUpdateInput";
import { BankAccountFindManyArgs } from "../../bankAccount/base/BankAccountFindManyArgs";
import { BankAccount } from "../../bankAccount/base/BankAccount";
import { BankAccountWhereUniqueInput } from "../../bankAccount/base/BankAccountWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BankBranchControllerBase {
  constructor(
    protected readonly service: BankBranchService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BankBranch })
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBankBranch(
    @common.Body() data: BankBranchCreateInput
  ): Promise<BankBranch> {
    return await this.service.createBankBranch({
      data: {
        ...data,

        bankId: data.bankId
          ? {
              connect: data.bankId,
            }
          : undefined,
      },
      select: {
        address: true,

        bankId: {
          select: {
            id: true,
          },
        },

        code: true,
        contactPhoneNumber: true,
        createdAt: true,
        description: true,
        id: true,
        name: true,
        normalizedName: true,
        note: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BankBranch] })
  @ApiNestedQuery(BankBranchFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async bankBranches(@common.Req() request: Request): Promise<BankBranch[]> {
    const args = plainToClass(BankBranchFindManyArgs, request.query);
    return this.service.bankBranches({
      ...args,
      select: {
        address: true,

        bankId: {
          select: {
            id: true,
          },
        },

        code: true,
        contactPhoneNumber: true,
        createdAt: true,
        description: true,
        id: true,
        name: true,
        normalizedName: true,
        note: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BankBranch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async bankBranch(
    @common.Param() params: BankBranchWhereUniqueInput
  ): Promise<BankBranch | null> {
    const result = await this.service.bankBranch({
      where: params,
      select: {
        address: true,

        bankId: {
          select: {
            id: true,
          },
        },

        code: true,
        contactPhoneNumber: true,
        createdAt: true,
        description: true,
        id: true,
        name: true,
        normalizedName: true,
        note: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BankBranch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBankBranch(
    @common.Param() params: BankBranchWhereUniqueInput,
    @common.Body() data: BankBranchUpdateInput
  ): Promise<BankBranch | null> {
    try {
      return await this.service.updateBankBranch({
        where: params,
        data: {
          ...data,

          bankId: data.bankId
            ? {
                connect: data.bankId,
              }
            : undefined,
        },
        select: {
          address: true,

          bankId: {
            select: {
              id: true,
            },
          },

          code: true,
          contactPhoneNumber: true,
          createdAt: true,
          description: true,
          id: true,
          name: true,
          normalizedName: true,
          note: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BankBranch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBankBranch(
    @common.Param() params: BankBranchWhereUniqueInput
  ): Promise<BankBranch | null> {
    try {
      return await this.service.deleteBankBranch({
        where: params,
        select: {
          address: true,

          bankId: {
            select: {
              id: true,
            },
          },

          code: true,
          contactPhoneNumber: true,
          createdAt: true,
          description: true,
          id: true,
          name: true,
          normalizedName: true,
          note: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/bankAccounts")
  @ApiNestedQuery(BankAccountFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BankAccount",
    action: "read",
    possession: "any",
  })
  async findBankAccounts(
    @common.Req() request: Request,
    @common.Param() params: BankBranchWhereUniqueInput
  ): Promise<BankAccount[]> {
    const query = plainToClass(BankAccountFindManyArgs, request.query);
    const results = await this.service.findBankAccounts(params.id, {
      ...query,
      select: {
        accountNumber: true,

        bankBranchId: {
          select: {
            id: true,
          },
        },

        bankId: {
          select: {
            id: true,
          },
        },

        code: true,
        createdAt: true,
        description: true,
        epan: true,
        id: true,
        name: true,
        normalizedName: true,
        note: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/bankAccounts")
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "update",
    possession: "any",
  })
  async connectBankAccounts(
    @common.Param() params: BankBranchWhereUniqueInput,
    @common.Body() body: BankAccountWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      bankAccounts: {
        connect: body,
      },
    };
    await this.service.updateBankBranch({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/bankAccounts")
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "update",
    possession: "any",
  })
  async updateBankAccounts(
    @common.Param() params: BankBranchWhereUniqueInput,
    @common.Body() body: BankAccountWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      bankAccounts: {
        set: body,
      },
    };
    await this.service.updateBankBranch({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/bankAccounts")
  @nestAccessControl.UseRoles({
    resource: "BankBranch",
    action: "update",
    possession: "any",
  })
  async disconnectBankAccounts(
    @common.Param() params: BankBranchWhereUniqueInput,
    @common.Body() body: BankAccountWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      bankAccounts: {
        disconnect: body,
      },
    };
    await this.service.updateBankBranch({
      where: params,
      data,
      select: { id: true },
    });
  }
}
